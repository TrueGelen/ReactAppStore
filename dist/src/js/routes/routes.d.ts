import type { TRout, TRoutMap } from './routes.types';
export declare const routes: TRout[];
export declare const routesMap: TRoutMap;
export declare const urlBuilder: (name: string, id: string) => string;
